export default function dateTimeFormatter(dateTimeString) {
  const date = dateTimeString.slice(0, 10)
  const time = dateTimeString.slice(11, 16)

  const ordinalDate = (date) => {
   const dayNum = date.slice() 
  }
}